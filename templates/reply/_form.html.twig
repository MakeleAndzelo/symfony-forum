{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.body) }}
    </div>

    <div class="form-group">
        <button type="submit" class="btn btn-primary">Add</button>
    </div>
{{ form_end(form) }}
